xmltodict
requests
pydantic[email]
# Ships the IANA zone database so TZID resolution behaves the same on
# Windows and slim containers, which carry no system zoneinfo.
tzdata
# RRULE evaluation for recurrence expansion.
python-dateutil
# ASGI framework for the browser view and, later, the DAV server.
starlette
uvicorn
# Default storage backend: a filesystem inside one SQLite file.
aloelite
